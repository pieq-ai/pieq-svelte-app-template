generator client {
  provider = "prisma-client"
  output   = "../src/lib/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

/// Salary Component master — component_type is validated as 'earning' | 'deduction' at the application layer
model SalaryComponent {
  /// Auto-incrementing surrogate primary key (internal use only)
  id             BigInt   @id @default(autoincrement())

  /// Externally-exposed, non-guessable identifier — used in all API routes
  cuid           String   @unique @default(cuid(2)) @db.Text

  name String   @db.VarChar(150)

  /// Stored as plain string: 'earning' | 'deduction' — validated at application layer
  component_type String   @db.VarChar(20)

  is_taxable     Boolean  @default(false)

  is_active      Boolean  @default(true)

  created_at     DateTime @default(now()) @db.Timestamptz()

  /// cuid of the user who created this record
  created_by     String?  @db.Text

  updated_at     DateTime @updatedAt @db.Timestamptz()

  /// cuid of the user who last updated this record
  updated_by     String?  @db.Text

  @@unique([name])

  @@map("salary_components")
}


model SystemRoles {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  name String @unique @db.VarChar(100)

  status          Boolean          @default(true)

  created_at DateTime @default(now()) @db.Timestamptz()
  created_by String?  @db.VarChar(100)
  updated_at DateTime @updatedAt @db.Timestamptz()
  updated_by String?  @db.VarChar(100)
  @@map("system_roles")
}

model Department {
  id        BigInt    @id @default(autoincrement())
  cuid     String @unique @default(cuid())
  name String @unique @db.VarChar(150)

  status Boolean @default(true)

  created_at DateTime @default(now()) @db.Timestamptz()
  created_by String?  @db.VarChar(100)
  updated_at DateTime @updatedAt @db.Timestamptz()
  updated_by String?  @db.VarChar(100)
  @@map("departments")
}

model Permissions {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  permission_key String @unique @db.VarChar(100)

  status          Boolean          @default(true)

  created_at DateTime @default(now()) @db.Timestamptz()
  created_by String?  @db.VarChar(100)
  updated_at DateTime @updatedAt @db.Timestamptz()
  updated_by String?  @db.VarChar(100)
  @@map("permissions")
}

model RolePermission {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  system_role_cuid String
  permission_cuid String

  created_at DateTime @default(now()) @db.Timestamptz()
  created_by String?  @db.VarChar(100)
  updated_at DateTime @updatedAt @db.Timestamptz()
  updated_by String?  @db.VarChar(100)

  @@unique([system_role_cuid, permission_cuid])
  @@map("role_permissions")
}

model BloodGroup {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  name String     @unique @db.VarChar(5)

  created_at DateTime @default(now()) @db.Timestamptz()
  created_by String?  @db.VarChar(100)
  updated_at DateTime @updatedAt @db.Timestamptz()
  updated_by String?  @db.VarChar(100)
  @@map("blood_groups")
}

model Designation {
  id               BigInt    @id @default(autoincrement())
  cuid            String @unique @default(cuid())
  name String @unique @db.VarChar(150)

  status Boolean @default(true)

  created_at DateTime @default(now()) @db.Timestamptz()
  created_by String?  @db.VarChar(100)
  updated_at DateTime @updatedAt @db.Timestamptz()
  updated_by String?  @db.VarChar(100)
  @@map("designations")
}

model PayGrade {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  name String @unique @db.VarChar(100)

  status Boolean @default(true)

  created_at DateTime @default(now()) @db.Timestamptz()
  created_by String?  @db.VarChar(100)
  updated_at DateTime @updatedAt @db.Timestamptz()
  updated_by String?  @db.VarChar(100)
  @@map("pay_grades")
}

model Nationality {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  name String @unique @db.VarChar(100)

  status    Boolean    @default(true)

  created_at DateTime @default(now()) @db.Timestamptz()
  created_by String?  @db.VarChar(100)
  updated_at DateTime @updatedAt @db.Timestamptz()
  updated_by String?  @db.VarChar(100)
  @@map("nationalities")
}

model EmploymentType {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  name String @unique @db.VarChar(50)

  status Boolean @default(true)

  created_at DateTime @default(now()) @db.Timestamptz()
  created_by String?  @db.VarChar(100)
  updated_at DateTime @updatedAt @db.Timestamptz()
  updated_by String?  @db.VarChar(100)
  @@map("employment_types")
}

model RelationType {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  name String @unique @db.VarChar(50)

  status    Boolean    @default(true)

  created_at DateTime @default(now()) @db.Timestamptz()
  created_by String?  @db.VarChar(100)
  updated_at DateTime @updatedAt @db.Timestamptz()
  updated_by String?  @db.VarChar(100)
  @@map("relation_types")
}

model DocumentType {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  name String @unique @db.VarChar(100)

  status Boolean @default(true)

  created_at DateTime @default(now()) @db.Timestamptz()
  created_by String?  @db.VarChar(100)
  updated_at DateTime @updatedAt @db.Timestamptz()
  updated_by String?  @db.VarChar(100)
  @@map("document_types")
}

model State {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  country_cuid String

  name String @db.VarChar(100)

  created_at DateTime @default(now()) @db.Timestamptz()
  created_by String?  @db.VarChar(100)
  updated_at DateTime @updatedAt @db.Timestamptz()
  updated_by String?  @db.VarChar(100)

  @@unique([country_cuid, name])
  @@index([country_cuid])
  @@map("states")
}

model Country {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  name String  @unique @db.VarChar(100)

  created_at DateTime @default(now()) @db.Timestamptz()
  created_by String?  @db.VarChar(100)
  updated_at DateTime @updatedAt @db.Timestamptz()
  updated_by String?  @db.VarChar(100)
  @@map("countries")
}

model Skills {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  name String @unique @db.VarChar(100)

  status Boolean @default(true)

  created_at DateTime @default(now()) @db.Timestamptz()
  created_by String?  @db.VarChar(100)
  updated_at DateTime @updatedAt @db.Timestamptz()
  updated_by String?  @db.VarChar(100)
  @@map("skills")
}

model AttendanceSource {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  name String @unique @db.VarChar(100)

  status Boolean @default(true)

  created_at DateTime @default(now()) @db.Timestamptz()
  created_by String?  @db.VarChar(100)
  updated_at DateTime @updatedAt @db.Timestamptz()
  updated_by String?  @db.VarChar(100)
  @@map("attendance_sources")
}

model Languages {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  name String @unique @db.VarChar(100)

  status Boolean @default(true)

  created_at DateTime @default(now()) @db.Timestamptz()
  created_by String?  @db.VarChar(100)
  updated_at DateTime @updatedAt @db.Timestamptz()
  updated_by String?  @db.VarChar(100)
  @@map("languages")
}

model Employee {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  // Employee Identity
  emp_code String @unique @db.VarChar(50)

  first_name String @db.VarChar(100)
  last_name  String @db.VarChar(100)

  father_name String? @db.VarChar(150)

  // Personal Details
  dob DateTime? @db.Date

  gender String? @db.VarChar(20)

  marital_status String? @db.VarChar(20)

  blood_group_cuid String?

  nationality_cuid String?

  // Contact
  mobile_no String? @db.VarChar(20)

  personal_email String? @db.VarChar(255)

  // Government IDs
  aadhar_no String? @db.VarChar(20)

  pan_no String? @db.VarChar(20)

  uan_no String? @db.VarChar(30)

  esi_no String? @db.VarChar(30)

  // Emergency Contact
  emergency_contact_name String? @db.VarChar(150)

  emergency_contact_no String? @db.VarChar(20)

  relation_cuid String?

  // HR Remarks
  remarks String? @db.Text

  // Onboarding Tracking
  profile_completion_status String @default("pending") @db.VarChar(30)

  // Audit
  created_at DateTime @default(now()) @db.Timestamptz()

  created_by String? @db.VarChar(100)

  updated_at DateTime @updatedAt @db.Timestamptz()

  updated_by String? @db.VarChar(100)

  @@map("employees")
}

model Employment {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  employee_cuid String

  department_cuid String

  role_cuid String?

  designation_cuid String

  pay_grade_cuid String?

  employment_type_cuid String?

  location_cuid String?

  reporting_manager_cuid String?

  employment_status String @default("onboarding") @db.VarChar(30)

  date_of_joining DateTime? @db.Date

  confirmation_date DateTime? @db.Date

  relieving_date DateTime? @db.Date

  official_email String? @db.VarChar(255)

  created_at DateTime @default(now()) @db.Timestamptz()

  created_by String? @db.VarChar(100)

  updated_at DateTime @updatedAt @db.Timestamptz()

  updated_by String? @db.VarChar(100)

  @@index([employee_cuid])
  @@index([department_cuid])
  @@index([designation_cuid])
  @@index([role_cuid])

  @@map("employments")
}

model EmployeeSkill {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  employee_cuid String

  skill_cuid String

  proficiency_level String? @db.VarChar(20)

  years_of_experience Decimal? @db.Decimal(5,2)

  created_at DateTime @default(now()) @db.Timestamptz()

  created_by String? @db.VarChar(100)

  updated_at DateTime @updatedAt @db.Timestamptz()

  updated_by String? @db.VarChar(100)

  @@unique([employee_cuid, skill_cuid])

  @@map("employee_skills")
}

model EmployeeLanguage {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  employee_cuid String

  language_cuid String

  proficiency_level String? @db.VarChar(20)

  can_read Boolean @default(false)

  can_write Boolean @default(false)

  can_speak Boolean @default(false)

  created_at DateTime @default(now()) @db.Timestamptz()

  created_by String? @db.VarChar(100)

  updated_at DateTime @updatedAt @db.Timestamptz()

  updated_by String? @db.VarChar(100)

  @@unique([employee_cuid, language_cuid])

  @@map("employee_languages")
}

model EmployeeEducation {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  employee_cuid String

  education_level String @db.VarChar(30)

  specialization String? @db.VarChar(150)

  institution String? @db.VarChar(255)

  university_board String? @db.VarChar(255)

  percentage Decimal? @db.Decimal(5,2)

  completed_at DateTime? @db.Date

  created_at DateTime @default(now()) @db.Timestamptz()

  created_by String? @db.VarChar(100)

  updated_at DateTime @updatedAt @db.Timestamptz()

  updated_by String? @db.VarChar(100)

  @@index([employee_cuid])

  @@map("employee_educations")
}

model EmployeeExperience {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  employee_cuid String

  company_name String @db.VarChar(255)

  role String? @db.VarChar(150)

  description String? @db.Text

  from_date DateTime? @db.Date

  to_date DateTime? @db.Date

  created_at DateTime @default(now()) @db.Timestamptz()

  created_by String? @db.VarChar(100)

  updated_at DateTime @updatedAt @db.Timestamptz()

  updated_by String? @db.VarChar(100)

  @@index([employee_cuid])

  @@map("employee_experiences")
}

model EmployeeDocument {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  employee_cuid String

  document_type_cuid String

  mime_type String? @db.VarChar(100)

  file_name String? @db.VarChar(255)

  file_size BigInt?

  document Bytes?

  created_at DateTime @default(now()) @db.Timestamptz()
  created_by String? @db.VarChar(100)

  updated_at DateTime @updatedAt @db.Timestamptz()
  updated_by String? @db.VarChar(100)

  @@index([employee_cuid])

  @@map("employee_documents")
}

model Address {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  employee_cuid String

  address_type String @default("communication") @db.VarChar(30)

  door_no String? @db.VarChar(50)

  address_line1 String @db.VarChar(255)

  address_line2 String? @db.VarChar(255)

  city String @db.VarChar(100)

  state_cuid String

  country_cuid String

  pin_code String? @db.VarChar(20)

  created_at DateTime @default(now()) @db.Timestamptz()
  created_by String? @db.VarChar(100)

  updated_at DateTime @updatedAt @db.Timestamptz()
  updated_by String? @db.VarChar(100)

  @@index([employee_cuid])

  @@map("addresses")
}

model BankDetail {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  employee_cuid String

  account_holder_name String @db.VarChar(150)

  account_number String @db.VarChar(50)

  bank_name String @db.VarChar(150)

  branch_name String? @db.VarChar(150)

  ifsc_code String @db.VarChar(20)

  is_primary Boolean @default(true)

  created_at DateTime @default(now()) @db.Timestamptz()
  created_by String? @db.VarChar(100)

  updated_at DateTime @updatedAt @db.Timestamptz()
  updated_by String? @db.VarChar(100)

  @@index([employee_cuid])

  @@map("bank_details")
}

model Role {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  name String @unique @db.VarChar(150)

  status Boolean @default(true)

  created_at DateTime @default(now()) @db.Timestamptz()
  created_by String? @db.VarChar(100)

  updated_at DateTime @updatedAt @db.Timestamptz()
  updated_by String? @db.VarChar(100)

  @@map("roles")
}

model Shift {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  shift_name String @unique @db.VarChar(100)

  start_time DateTime @db.Time(6)

  end_time DateTime @db.Time(6)

  minimum_work_hours Decimal @db.Decimal(4, 2)

  status Boolean @default(true)

  created_by String? @db.VarChar(255)

  updated_by String? @db.VarChar(255)

  created_at DateTime @default(now()) @db.Timestamptz()

  updated_at DateTime @updatedAt @db.Timestamptz()

  @@map("shifts")
}

model CompanyLocation {
  id BigInt @id @default(autoincrement())

  cuid String @unique @default(cuid())

  name String @unique @db.VarChar(150)

  address_line1 String @db.VarChar(255)
  address_line2 String? @db.VarChar(255)

  city String @db.VarChar(100)

  state_cuid String
  country_cuid String

  pin_code String @db.VarChar(15)

  timezone String @db.VarChar(100)

  status Boolean @default(true)

  created_at DateTime @default(now()) @db.Timestamptz()
  created_by String? @db.VarChar(100)

  updated_at DateTime @updatedAt @db.Timestamptz()
  updated_by String? @db.VarChar(100)

  @@map("company_locations")
}

model LeaveType {
  id                BigInt   @id @default(autoincrement())
  cuid              String   @unique @default(cuid())
  leave_name        String   @unique @db.VarChar(100)
  leave_code        String   @unique @db.VarChar(20)
  description       String?  @db.Text
  is_paid           Boolean  @default(true)
  requires_approval Boolean  @default(true)
  status            Boolean  @default(true)
  created_at        DateTime @default(now()) @db.Timestamptz(3)
  updated_at        DateTime @default(now()) @updatedAt @db.Timestamptz(3)
  created_by        String?  @db.Text
  updated_by        String?  @db.Text

  @@map("leave_types")
}

model LeavePolicy {
  id                       BigInt                 @id @default(autoincrement())
  cuid                     String                 @unique @default(cuid())

  leave_type_cuid          String                 @db.Text

  annual_limit             Decimal                @db.Decimal(5, 1)
  max_per_month            Decimal?               @db.Decimal(4, 1)
  carry_forward_allowed    Boolean                @default(false)
  max_carry_forward_days   Decimal?               @db.Decimal(4, 1)
  document_required        Boolean                @default(false)
  document_required_after_days Int?
  min_service_days         Int                    @default(0)
  allow_half_day           Boolean                @default(false)
  gender_specific          Boolean                @default(false)
  applicable_gender        String?
  status                   Boolean                @default(true)
  deactivated_by_leave_type Boolean                @default(false)
  created_at               DateTime               @default(now()) @db.Timestamptz(3)
  updated_at               DateTime               @default(now()) @updatedAt @db.Timestamptz(3)
  created_by               String?                @db.Text
  updated_by               String?                @db.Text

  @@index([leave_type_cuid])

  @@map("leave_policies")
}



model LeavePolicyEmploymentType {
  id                     BigInt   @id @default(autoincrement())
  cuid                   String   @unique @default(cuid())

  leave_policy_cuid      String   @db.Text
  employment_type_cuid   String   @db.Text
  created_at             DateTime @default(now()) @db.Timestamptz(3)
  updated_at             DateTime @default(now()) @updatedAt @db.Timestamptz(3)
  created_by             String?  @db.Text
  updated_by             String?  @db.Text

  @@unique([leave_policy_cuid, employment_type_cuid])

  @@index([leave_policy_cuid])
  @@index([employment_type_cuid])

  @@map("leave_policy_employment_types")
}

model LeaveBalance {
  id                    BigInt   @id @default(autoincrement())
  cuid                  String   @unique @default(cuid())

  employee_cuid         String   @db.Text
  leave_type_cuid       String   @db.Text

  year                  Int

  allocated_days        Decimal  @default(0) @db.Decimal(8, 2)
  used_days             Decimal  @default(0) @db.Decimal(8, 2)
  remaining_days        Decimal  @default(0) @db.Decimal(8, 2)
  carried_forward_days  Decimal  @default(0) @db.Decimal(8, 2)

  created_at            DateTime @default(now()) @db.Timestamptz(3)
  updated_at            DateTime @default(now()) @updatedAt @db.Timestamptz(3)

  created_by            String?  @db.Text
  updated_by            String?  @db.Text

  @@unique([employee_cuid, leave_type_cuid, year])

  @@index([employee_cuid])
  @@index([leave_type_cuid])
  @@index([year])

  @@map("leave_balances")
}

model LeaveRequest {
  id                    BigInt   @id @default(autoincrement())
  cuid                  String   @unique @default(cuid())

  employee_cuid         String   @db.Text
  leave_type_cuid       String   @db.Text

  start_date            DateTime @db.Date
  end_date              DateTime @db.Date

  total_days            Decimal  @db.Decimal(8, 2)

  is_half_day           Boolean  @default(false)
  half_day_session      String?  @db.VarChar(10) // FN / AN

  reason                String?  @db.Text
  document_url          String?  @db.Text

  request_status        String   @default("pending") @db.VarChar(30)

  days_from_primary     Decimal? @db.Decimal(8, 2)
  days_from_lwp         Decimal? @db.Decimal(8, 2)

  approved_by           String?  @db.Text
  approved_at           DateTime? @db.Timestamptz(3)

  rejected_by           String?  @db.Text
  rejected_at           DateTime? @db.Timestamptz(3)

  withdrawn_at          DateTime? @db.Timestamptz(3)

  created_at            DateTime @default(now()) @db.Timestamptz(3)
  updated_at            DateTime @default(now()) @updatedAt @db.Timestamptz(3)

  created_by            String?  @db.Text
  updated_by            String?  @db.Text

  @@index([employee_cuid])
  @@index([leave_type_cuid])
  @@index([request_status])

  @@map("leave_requests")
}

model AttendanceRecord {
  id                     BigInt   @id @default(autoincrement())
  cuid                   String   @unique @default(cuid())

  employee_cuid          String   @db.Text

  attendance_date        DateTime @db.Date

  check_in_time          DateTime?
  check_out_time         DateTime?

  work_duration_minutes  Int?

  attendance_status      String   @default("Absent") @db.VarChar(30)

  attendance_source_id   String?  @db.Text

  remarks                String?  @db.Text

  created_at             DateTime @default(now()) @db.Timestamptz(3)
  updated_at             DateTime @default(now()) @updatedAt @db.Timestamptz(3)

  created_by             String?  @db.Text
  updated_by             String?  @db.Text

  @@unique([employee_cuid, attendance_date])

  @@index([employee_cuid])
  @@index([attendance_date])

  @@map("attendance_records")
}